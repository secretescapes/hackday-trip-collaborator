import { VotedSalesPipe } from './voted-sales.pipe';

describe('VotedSalesPipe', () => {
  it('create an instance', () => {
    const pipe = new VotedSalesPipe();
    expect(pipe).toBeTruthy();
  });
});
